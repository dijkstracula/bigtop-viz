import * as React from 'react'

interface Props {
  inputFile: string
}

export const FileUpload = (props: Props) => {
  return (
    <div>
      <h1>Hello, world!</h1>
      <p>Upload a Cartpoles file to begin.</p>
      <p>
      </p>
    </div>
  )
}